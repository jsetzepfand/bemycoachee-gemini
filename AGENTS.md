- project path is /Users/setzejrg/Werkstatt/own/bemycoachee-gemini
- create a coaching app where the user can chat with an AI coach.
- Frontend is done in Vue.js
- The Frontend should integrate with a REST API backend that saves chat messages
- The user enters a chat message into the frontend
- After a user enters a message the frontend passes the message on to an AI
- message and answer are then passed to the REST API backend and saved into the database

- for local development backend should be mocked and data saved in a file or something
- 
